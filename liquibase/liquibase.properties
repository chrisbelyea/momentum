# Defaults; override via env vars for target DB
changeLogFile=liquibase/changelog/changelog-master.yaml

# Default: SQLite for local dev/testing
url=jdbc:sqlite:momentum-dev.db
driver=org.sqlite.JDBC

# Override for PostgreSQL (via env vars in CI/prod):
# export LIQUIBASE_URL=jdbc:postgresql://localhost:5432/momentum
# export LIQUIBASE_USERNAME=momentum
# export LIQUIBASE_PASSWORD=momentum
# export LIQUIBASE_DRIVER=org.postgresql.Driver
