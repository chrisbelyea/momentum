# Liquibase temporary installation (for local testing)
liquibase-install/
liquibase.zip

# Database files (for local testing)
*.db
*.db-journal

# Build artifacts
/bin/
/dist/
momentum-server
server

# Browser E2E dependencies
node_modules/
